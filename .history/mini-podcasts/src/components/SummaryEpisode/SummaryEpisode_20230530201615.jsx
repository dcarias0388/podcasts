import React from 'react';
import PropTypes from 'prop-types';

const SummaryEpisode = ({data}) => {
  return (
    <div>SummaryEpisode</div>
  )
}

SummaryEpisode.propTypes = {
    data: PropTypes.object,
};

export default SummaryEpisode;
